rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // רק משתמש מחובר (שהוגדר ב-Firebase Authentication) יכול לקרוא/לכתוב
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
