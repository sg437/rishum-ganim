# כיסוי העיצוב מול המערכת — מפת המימוש

נבדק ב-2026-09-08 מול `management.html` (מערך `GROUPS` בניווט ומערך `MODULES`).
זהו המסמך שפותחים לפני שמתחילים להלביש את העיצוב על התוכנה.

## המספרים

| | |
| --- | --- |
| פריטי ניווט במערכת האמיתית | **32** |
| מהם עוצבו | **32** — 25 מסכים + 7 דרך `Soon` |
| מסכי עיצוב בסך הכל | **27** (25 + `Login` + `Soon`) |

16 המודולים של מחלקת הרישום (מערך `MODULES`) מכוסים **במלואם**.

## עוצב — 19 מסכים

| מפתח בקוד | מסך העיצוב |
| --- | --- |
| — | `Login` (מסך הכניסה — אינו פריט ניווט) |
| `home` | `Home` |
| `students` | `Students` |
| `staff` | `Staff` |
| `map` | `PlacementMap` |
| `import` | `Import` |
| `channels` | `Channels` |
| `distribution` | `Publishing` |
| `smoove` | `Distribution` |
| `requests` | `Appeals` |
| `followups` | `Triage` |
| `license` | `Licensing` |
| `ministry` | `Compliance` |
| `breakdowns` | `Analytics` |
| `comm` | `Messages` |
| `circulars` | `Circulars` |
| `decisions` | `Decisions` |
| `perms` | `Settings` |
| `assistant` | `Assistant` |

## לא עוצב — כבר לא קיים

שבעת המסכים החיים (`registration` · `place` · `cities` · `activities` ·
`overview` · `reports` · `guide`) עוצבו בסבב v6, וכך גם `Soon` — רכיב אחד
שמשרת את שבעת פריטי "יופעל בהמשך" (`crm` · `portal` · `billing` · `docs` ·
`hours` · `spec` · `plan`).

**כל 32 פריטי הניווט מכוסים:** 25 מסכים מעוצבים + `Login` + `Soon`.

## פער מבני בניווט

**נסגר ב-v6.** הניווט בעיצוב אומץ מהתוכנה: 8 קבוצות, 32 פריטים, אותם
מפתחות ואותה חלוקה. הפירוט המלא ב-`DESIGN-SPEC.md`, סעיף "הסרגל".

## פערים פתוחים בקבצי העיצוב

1. **מספר הגנים אינו אחיד** — `Compliance` אומר 96, `Analytics` אומר 214,
   והעוגן הוא 122. נתון שחוזר במסכים רבים; צריך ליישר לפני המימוש.
2. ~~מודאל השיבוץ האוטומטי ב-`Students`~~ — הוחזר ב-v6.
3. ~~נתוני העוגן~~ — הוכרעו: 4,308 / 5,250. הטבלה המלאה ב-`DESIGN-SPEC.md`.

## מה שאין בו פער

- כל 19 המסכים שלמים: תגיות מאוזנות, נגמרים ב-`</html>`, בלי קטיעות.
- לכל 19 יש ארטבורד דסקטופ 1280×760, ארטבורד מובייל 390×844, ומצב לילה.
- 34 טוקני הצבע זהים בכל המסכים, ביום ובלילה.
- אין חריגה בסולם הטיפוגרפיה, ברדיוסים או בפלטה.
