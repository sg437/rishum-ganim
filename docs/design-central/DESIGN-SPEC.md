# DESIGN-SPEC — המערכת המרכזית (management.html)

> **הקובץ הזה ריק בכוונה.** הוא ממתין למפרט שייצא משלב ב' של עבודת העיצוב.
>
> **מה זה:** מקור האמת היחיד לשפה העיצובית של המרכזי. כל שיחת עיצוב חדשה
> מתחילה בהדבקת התוכן שלו, ולכן הוא חייב לעמוד בפני עצמו — בלי "כפי שסיכמנו",
> בלי הפניות לשיחה כלשהי, וכל ערך מספרי מפורש.
>
> **מה זה לא:** אינו נוגע לתוכנת הערים (`index.html` · `ui-lab.css` · `ui-lab.js`)
> ואינו נוגע ל-`docs/design/`, שהיא חבילת העיצוב של הערים. ראה ההערה
> ב-`management.html:31-35`.

**סטטוס:** טרם מולא · **פלטה שנבחרה:** — · **עודכן לאחרונה:** —

---

## איך עובדים עם הקובץ

1. בסוף שלב ב' מדביקים לכאן את המפרט המלא, במקום הסעיפים הריקים למטה.
2. בכל שיחת עיצוב חדשה מדביקים את התוכן בראש השיחה, תחת הכותרת `═══ DESIGN-SPEC ═══`.
3. בסוף כל קבוצת מסכים — אם נוסף רכיב או ערך שלא היה כאן, **מעדכנים מיד**,
   לפני השיחה הבאה. מפרט שלא מתעדכן הוא מקור הסחף.
4. כל שינוי — לרשום בטבלת השינויים בתחתית.

---

## 1. טוקני צבע

<!-- שם המשתנה · ערך HEX · למה הוא משמש -->

## 2. טיפוגרפיה

<!-- משפחת גופן, ולכל רמה: גודל · משקל · גובה שורה
     (כותרת מסך, כותרת קטע, כותרת כרטיס, טקסט, טקסט משני, תווית, מספר) -->

## 3. סולם ריווח

<!-- הערכים המותרים בלבד -->

## 4. רדיוסים

<!-- הערכים המותרים בלבד, ולאיזה רכיב כל אחד -->

## 5. סולם הצללות

<!-- שלוש רמות · ערך CSS מלא לכל אחת · מתי משתמשים בכל רמה -->

## 6. רכיבים

<!-- כפתור (ראשי/משני/הרסני), שדה קלט, בורר, טבלה, כרטיס, תגית סטטוס,
     drawer, popover, מודאל, טוסט.
     לכל רכיב: מידות + כל המצבים — רגיל, ריחוף, לחוץ, פוקוס, מושבת, שגיאה -->

## 7. מצבי מסך

<!-- טעינה · ריק · שגיאה · אין הרשאה -->

## 8. פריסה וניווט

<!-- רוחב הסרגל (פתוח/מכווץ) · רוחב מקסימלי לתוכן · נקודת השבירה 900px ·
     התנהגות המובייל (מגירה + bottom sheet) -->

## 9. כללי RTL

<!-- dir="rtl" בכל פריסה · מספרים, אימיילים וטלפונים ב-dir="ltr" -->

## 10. כללי ביצועים

<!-- טבלאות ארוכות · אנימציות 150-200ms על opacity ו-transform בלבד ·
     איסור backdrop-filter מעל טבלה · איסור perspective / rotate3d / WebGL ·
     גופנים מקומיים בלבד מ-fonts/ (CSP: font-src 'self' data:) -->

---

## טבלת שינויים

| תאריך | מה השתנה | באיזו שיחה/מסך התגלה |
|---|---|---|
| | | |
