# הפעלת המערכת בענן — מדריך הגדרה (חד-פעמי) 🔧

הגרסה המתארחת (`index.html`) שומרת את כל הנתונים ב**ענן** (Firebase) עם **התחברות** (אימייל וסיסמה או **חשבון Google**),
כך שאותם נתונים נראים בכל המכשירים — טלפון ומחשב — ומסתנכרנים בזמן אמת.

צריך לבצע פעם אחת את השלבים הבאים (בערך 10–15 דקות). אין צורך בכרטיס אשראי — השכבה החינמית מספיקה בהרבה.

---

## שלב 1 — יצירת פרויקט Firebase
1. היכנס/י ל־https://console.firebase.google.com עם חשבון Google.
2. לחצ/י **Add project / הוספת פרויקט** → תן/י שם (למשל `rishum-ganim`) → המשך/י (אפשר לכבות Analytics) → **Create**.

## שלב 2 — הפעלת התחברות (Authentication)
1. בתפריט הצד: **Build → Authentication → Get started**.
2. בלשונית **Sign-in method** → בחר/י **Email/Password** → **Enable** → **Save**.
3. בלשונית **Users → Add user**: הוסף/י את המשתמש/ים שיתחברו למערכת (אימייל + סיסמה).
   כל מי שתוסיף/י כאן יוכל להתחבר. (אין הרשמה פתוחה — רק מי שהוספת.)

### הפעלת "התחברות עם Google" (מומלץ) 🆕
כדי שכפתור **"התחברות עם Google"** במסך הכניסה יעבוד:
1. באותה לשונית **Sign-in method** → **Add new provider** → **Google** → **Enable**.
2. בחר/י **Project support email** (מייל התמיכה של הפרויקט) → **Save**.
3. חשוב לאישור הדומיין: ודא/י שהדומיין של האתר מופיע ב-**Authentication → Settings → Authorized domains** (ראה שלב 6). בלי זה חלון ה-Google ייחסם.
> **מי מורשה להיכנס עם Google?** רק כתובות אימייל שכבר **מוגדרות במערכת** — כלומר מנהלי מערכת, כתובות שכבר ערכו נתונים, או רשימת "מיילים מורשים". מי שינסה להיכנס עם Google בכתובת שאינה מוכרת — יקבל הודעה "האימייל אינו מורשה" וינותק אוטומטית.
> **טיפ אבטחה (לשלב מתקדם):** האכיפה הזו נעשית בצד הלקוח. לאכיפה חזקה ברמת מסד הנתונים (למשל בהרחבה למספר ערים) — מומלץ לעבור לכללי הרשאה מבוססי-מייל או ל-Supabase עם RLS.

## שלב 3 — יצירת מסד הנתונים (Firestore)
1. בתפריט: **Build → Firestore Database → Create database**.
2. בחר/י מיקום (למשל `eur3`/`europe-west`) → **Next**.
3. בחר/י **Start in production mode** → **Create**.
4. עבור/י ללשונית **Rules**, מחק/י את הקיים והדבק/י את הכללים הבאים, ואז **Publish**:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // רק משתמש מחובר (שהוגדר ב-Authentication) יכול לקרוא/לכתוב
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

> כללים אלה מבטיחים שרק משתמש שהתחבר עם אימייל/סיסמה שהגדרת יוכל לגשת לנתונים.

## שלב 4 — קבלת המפתחות (Config) והדבקתם באפליקציה
1. בקונסולה: גלגל השיניים ⚙️ (למעלה משמאל) → **Project settings**.
2. גלול/י ל־**Your apps** → לחצ/י על אייקון האינטרנט **</>** (Web) → תן/י כינוי → **Register app**.
3. יוצג בלוק `const firebaseConfig = { … }` עם 6 ערכים. העתק/י אותם.
4. פתח/י את הקובץ **`index.html`** במאגר, ובראש קוד ה-JavaScript החלף/י את בלוק `firebaseConfig`
   (השדות עם `YOUR_...`) בערכים האמיתיים שקיבלת. לדוגמה:

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "rishum-ganim.firebaseapp.com",
  projectId: "rishum-ganim",
  storageBucket: "rishum-ganim.firebasestorage.app",
  messagingSenderId: "1234567890",
  appId: "1:1234567890:web:abcdef"
};
```

5. שמור/י את הקובץ (commit).

## שלב 5 — פרסום כאתר (GitHub Pages)
1. במאגר ב-GitHub: **Settings → Pages**.
2. תחת **Build and deployment → Source** בחר/י **Deploy from a branch**.
3. בחר/י ענף **main** ותיקייה **/ (root)** → **Save**.
   (אם השינויים עדיין בענף הפיתוח — מזגו קודם את ה-Pull Request ל-main.)
4. אחרי דקה־שתיים יופיע הכתובת, בדרך כלל: **`https://sg437.github.io/rishum-ganim/`**

## שלב 6 — אישור הדומיין ב-Firebase (חשוב!)
1. חזור/י ל-Firebase → **Authentication → Settings → Authorized domains → Add domain**.
2. הוסף/י את `sg437.github.io` (הדומיין של GitHub Pages).
   בלי זה ההתחברות תיחסם בכתובת האמיתית. (`localhost` כבר מאושר לבדיקות מקומיות.)

---

## זהו! ✅
נכנסים לכתובת האתר, מתחברים עם האימייל והסיסמה שהגדרת בשלב 2, וכל הנתונים נשמרים בענן
ומסתנכרנים בין כל המכשירים.

### העברת הנתונים שכבר הוזנו בטלפון (מהגרסה הישנה)
1. בטלפון, פתח/י את הקישור הישן (הדמו) → **הגדרות → ⬇️ ייצוא גיבוי (JSON)** ושמור/י את הקובץ.
2. באפליקציה החדשה (בענן) → **הגדרות → ⬆️ שחזור מגיבוי** ובחר/י את הקובץ.
   הנתונים ייטענו ויעלו לענן, ומעכשיו יופיעו גם במחשב.

### שיתוף לביקורת עם מישהו חיצוני
אפשרות א׳ — לתת לו/ה משתמש: הוסף/י אותו ב-Authentication → Users (אימייל + סיסמה), והוא יתחבר לכתובת האתר.
אפשרות ב׳ — אם רוצים שרק *יראה* בלי חשבון: אפשר להקים בהמשך "מצב צפייה בלבד" (דורש התאמה נוספת — פנה/י אליי).

### עלות
השכבה החינמית של Firebase (Spark) כוללת בערך 50K קריאות ו-20K כתיבות ליום ו-1GB אחסון —
הרבה מעבר לצורך של רשת גני ילדות. אין חיוב ללא שדרוג יזום לתוכנית בתשלום.


---

# חיבור Google Drive — "גשר" חינמי (Google Apps Script) 📁

מאפשר לכל משתמש שמחובר לתוכנה (עם המייל שלו) ליצור תיקייה לכל ילדה, להעלות מסמכים, לצפות ולהוריד —
בלי חיבור Google אישי. הכל נשמר ב-Drive של חשבון אחד (הארגון), מסודר לפי שנה.

> אם ביצעת קודם שלבים של "OAuth client ID" (Drive API / Client ID) — הם כבר **לא נחוצים** בגישה הזו. אפשר להשאיר, לא מזיק.

## שלב G1 — יצירת הסקריפט
1. היכנס/י ל־https://script.google.com עם **חשבון ה-Drive של הארגון** (כל הקבצים ייווצרו שם!).
2. לחצ/י **New project**.
3. מחק/י את מה שכתוב ב-`Code.gs`, והדבק/י במקומו את **כל** התוכן של הקובץ **`APPS_SCRIPT.gs`** שנמצא במאגר.
4. שמור/י (אייקון הדיסקט 💾).

## שלב G2 — פרסום כ-Web App
5. למעלה מימין: **Deploy → New deployment**.
6. ליד "Select type" לחצ/י על גלגל השיניים ⚙️ ובחר/י **Web app**.
7. הגדר/י:
   - **Execute as:** `Me` (חשבון הארגון).
   - **Who has access:** `Anyone`.
8. לחצ/י **Deploy**. יופיע בקשת הרשאה → **Authorize access** → בחר/י את חשבון הארגון → אם יופיע מסך "unverified": **Advanced → Go to ... (unsafe) → Allow**.
9. יופיע **Web app URL** שמסתיים ב-`/exec`. העתק/י אותו ושלח/י לי.

## שלב G3 — שימוש
- אני מכניס את הכתובת לקוד ומעלה לאוויר. מאז:
- כל משתמש נכנס לתוכנה עם **המייל שלו** → בתיק הילדה: **"צור תיקייה בדרייב"** → **"העלאת מסמכים"**.
- הקבצים מופיעים עם **קישור ישיר ל-Drive** וכפתור **הורדה**.
- **הגדרות → חיבור Google Drive:** בדיקת חיבור, פתיחת תיקיית האב, ומתן גישה ישירה ל-Drive למשתמש לפי אימייל Google (למי שרוצה גם לפתוח קבצים ישירות ב-Drive).
- לשינוי שם תיקיית האב: ערוך/י את `ROOT_FOLDER_NAME` בראש `APPS_SCRIPT.gs`.

## שלב G4 — עדכון הגשר: חלוקה לחינוך רגיל/מיוחד + תיקיות צוות (פעם אחת) 🆕
> נדרש כדי שיעבדו שתי התכונות החדשות: **חלוקת הדרייב הראשי לחינוך רגיל / חינוך מיוחד** (סעיף 12)
> ו**תיקיית דרייב אישית לכל איש/אשת צוות** (סעיף 17). עד לעדכון — שאר המערכת עובדת כרגיל,
> אך יצירת תיקיות צוות / המבנה המחולק תיתן שגיאה.

1. פתח/י את פרויקט ה-Apps Script (script.google.com — **אותו פרויקט** של הגשר).
2. החלף/י את תוכן `Code.gs` ב**גרסה המעודכנת** של `APPS_SCRIPT.gs` מהמאגר
   (כוללת את `eduFolderName_`, את `childFolder_` עם פרמטר החינוך, ואת הפעולה החדשה `staffFolder_`).
3. **Deploy → Manage deployments →** עריכת הפריסה הקיימת (עיפרון ✏️) → **Version: New version → Deploy**.
   הכתובת הקיימת נשארת — אין צורך לשנות דבר ב-`index.html`.

לאחר העדכון, המבנה בדרייב יהיה:
```
<תיקיית האב>/
├── חינוך רגיל/
│   ├── תשפ"ז/  → תיקייה לכל ילדה (שם · ת"ז)
│   └── אנשי צוות/  → תיקייה לכל איש/אשת צוות (שם · ת"ז)
└── חינוך מיוחד/
    ├── תשפ"ז/  → …
    └── אנשי צוות/  → …
```

> **הערה חשובה:** תיקיות **ילדות שכבר נוצרו** נשארות במקומן הקודם (התוכנה פותחת אותן לפי המזהה
> השמור, ולא יוצרת חדשות). רק תיקיות **חדשות** ייווצרו במבנה המחולק. אם רוצים להעביר גם את הישנות —
> אפשר לגרור אותן ידנית ב-Drive לתיקיית החינוך המתאימה.

## שלב G5 — שיקוף הנתונים ל-Google Sheets (סעיף 12) 🆕
> מאפשר לשקף את **כל הנתונים** (תלמידות · גנים · צוות · שיבוץ) לקובץ **Google Sheets** בתיקיית האב —
> כגיבוי/דוחות שמתעדכן מהמערכת. הגיליון הוא **מראה** בלבד; מקור האמת נשאר Firestore.

1. עדכן/י את הגשר לגרסה האחרונה של `APPS_SCRIPT.gs` (כוללת את הפעולה החדשה `sheetSync`) —
   בדיוק כמו בשלב G4: החלפת תוכן `Code.gs` → **Deploy → Manage deployments → New version → Deploy**.
   בפעם הראשונה שתריץ/י שיקוף, Google יבקש הרשאה נוספת ל-Sheets — **Authorize access → Allow**.
2. בתוכנה: **☰ תפריט → הגדרות → חיבור Google Drive → 📊 שיקוף ל-Google Sheets**:
   - **"שקף עכשיו ל-Google Sheets"** — יוצר/מעדכן את הגיליון ומציג קישור **"פתח את הגיליון"**.
   - **"שיקוף אוטומטי"** (אופציונלי) — מסמן, וכל שינוי בנתונים מעדכן את הגיליון אוטומטית (בהשהיה קצרה).

הגיליון נוצר בתיקיית האב בשם **"רשת הגנים מודיעין עילית — נתונים (שיקוף)"**, עם לשונית לכל ישות.

> **טיפ:** לצריכה גבוהה (עדכונים תכופים) עדיף להשאיר את השיקוף האוטומטי **כבוי** ולהריץ ידנית מדי פעם —
> כדי לא להעמיס על מכסות ה-Apps Script החינמיות.


---

# ניהול משתמשים מתוך התוכנה 👥 (דרך גשר ה-Apps Script — **בלי תשלום**)

מאפשר ל**מנהל מערכת** להוסיף משתמשים, לאפס סיסמאות ולמחוק — **ישירות מתוך התוכנה**
(בלשונית **הגדרות → ניהול משתמשים**), בלי להיכנס לקונסולת Firebase. הכל עובר דרך אותו
גשר Apps Script של ה-Drive, **בלי Blaze ובלי כרטיס אשראי** — השירותים שבשימוש (Firebase
Authentication) חינמיים לגמרי.

יש שתי רמות:
- **הוספת משתמש — עובדת מיד** אחרי עדכון הגשר (שלב U1). לא צריך שום דבר נוסף.
- **רשימת משתמשים / מחיקה / איפוס סיסמה** — דורשות הגדרה חד-פעמית של Service Account (שלב U2).
  עד שמגדירים, המסך פשוט יציג הודעה מתאימה, והוספת משתמש תמשיך לעבוד.

## הרשאות
רק כתובות אימייל שמופיעות ברשימת **"מנהלי מערכת"** (הגדרות → מנהלי מערכת) יכולות לנהל משתמשים.
אם הרשימה ריקה — כל משתמש מחובר יכול (מצב אתחול), לכן **מומלץ להגדיר מנהל ראשון** תחילה.
הגשר מאמת זאת בעצמו מול Firestore, כך שאי אפשר לעקוף מהדפדפן.

## שלב U1 — עדכון הגשר (חובה, פעם אחת)
> דורש שכבר ביצעת את "גשר Drive" למעלה. אם עדיין לא — בצע/י אותו קודם.
1. פתח/י את פרויקט ה-Apps Script (script.google.com, אותו פרויקט של הגשר).
2. החלף/י את תוכן `Code.gs` ב**גרסה המעודכנת** של `APPS_SCRIPT.gs` מהמאגר (כוללת את פעולות המשתמשים).
3. ודא/י ש-`FIREBASE_PROJECT_ID` בראש הקובץ נכון (`rishum-ganim-fad40`).
4. **Deploy → Manage deployments →** עריכת הפריסה הקיימת → **Version: New version → Deploy**
   (כדי שהכתובת הקיימת תרוץ עם הקוד החדש; אין צורך לשנות את הכתובת ב-`index.html`).

מעכשיו: מנהל נכנס ל**הגדרות → ניהול משתמשים**, מזין אימייל + סיסמה ראשונית (6 תווים לפחות),
לוחץ **צור משתמש** — והמשתמש יכול להתחבר מיד. 🎉

## שלב U2 — Service Account (אופציונלי — לרשימה/מחיקה/איפוס סיסמה)
פעולות אלה דורשות הרשאת אדמין ל-Firebase Auth. משיגים אותה עם "חשבון שירות" (חינמי):

1. **הורדת מפתח:** בקונסולת Firebase → **⚙️ Project settings → Service accounts →
   Generate new private key → Generate key**. יורד קובץ JSON. שמור/י אותו בבטחה (זהו סוד!).
2. פתח/י את קובץ ה-JSON וקח/י ממנו שני ערכים: `client_email` ו-`private_key`.
3. ב-Apps Script: **Project Settings (⚙️ בצד) → Script Properties → Add script property**,
   והוסף/י שתי מאפיינים:
   - `SA_CLIENT_EMAIL` = הערך של `client_email` (למשל `firebase-adminsdk-xxxx@rishum-ganim-fad40.iam.gserviceaccount.com`)
   - `SA_PRIVATE_KEY` = הערך של `private_key` (כל הבלוק, כולל `-----BEGIN PRIVATE KEY-----`
     ו-`-----END PRIVATE KEY-----`. אפשר להדביק עם שורות ממשיות או עם `\n` — הקוד מטפל בשניהם).
4. שמור/י. (לא צריך לפרוס מחדש — Script Properties נקראות בזמן ריצה.)

> **אבטחה:** המפתח נשמר רק בצד השרת (Script Properties) ולעולם אינו נחשף לדפדפן.
> אל תדביק/י אותו בקוד עצמו ואל תעלה/י את קובץ ה-JSON למאגר.

מעכשיו, ב**הגדרות → ניהול משתמשים** יופיעו גם רשימת המשתמשים, **✏️ שם**, **🔑 איפוס סיסמה** ו-**🗑️ מחיקה**.
עמודת **"עריכה אחרונה בנתונים"** (מתי כל משתמש שינה נתונים לאחרונה) נאספת אוטומטית באפליקציה ואינה דורשת הגדרה.

## עלות
0 ₪. Firebase Authentication (יצירה/מחיקה/רשימה/איפוס סיסמה) כלול בשכבה החינמית (Spark),
ואין צורך ב-Blaze או בכרטיס אשראי. גם חשבון השירות חינמי.

> תמיד אפשר גם לנהל משתמשים ישירות בקונסולת Firebase → Authentication — הגשר רק מוסיף נוחות בתוך התוכנה.
